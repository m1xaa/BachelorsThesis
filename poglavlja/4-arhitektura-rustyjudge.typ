= Архитектура алата RustyJudge
<arhitektura-rustyjudge>

Ово поглавље описује како је RustyJudge организован као софтверски систем.
Претходно поглавље увело је појмове који се користе у статичкој анализи, док
се овде посматрају конкретни модули, улазне тачке и ток података кроз
имплементацију.

RustyJudge је реализован као Rust пројекат који садржи библиотечки део,
извршне програме, LSP сервер, benchmark окружење и VS Code проширење. Важна
архитектонска одлука је да се главна логика анализе налази у заједничкој
библиотеци. На тај начин CLI, LSP сервер и benchmark код не садрже сопствене
варијанте анализе, већ користе исти основни ток.

== Организација пројекта

Изворни код Rust дела алата налази се у директоријуму `src/`. Модули су
подељени према одговорностима. Улазни бинарни програми су мали и служе само да
покрену одговарајући режим рада. Главни део понашања налази се у библиотечким
модулима који се могу поново користити.

#figure(
  table(
    columns: (1.8fr, 4.2fr),
    table.header[*Модул или директоријум*][*Одговорност*],
    [`src/parser/`],
    [Омотач око `rslint_parser` библиотеке, читање JavaScript извора и
    враћање синтаксичког корена или грешке парсирања.],
    [`src/cfg/`],
    [Семантичко спуштање, табела симбола, CFG структуре, изградња графа и
    анализа живости.],
    [`src/rules/`],
    [Интерфејс правила, `RuleContext`, подразумевани скуп правила и
    појединачне имплементације правила.],
    [`src/linter/`],
    [Језгро које прави контекст, покреће правила и враћа дијагностике.],
    [`src/diagnostics/`],
    [Заједнички формат дијагностике који користе CLI и LSP слој.],
    [`src/cli/` и `src/run_cli/`],
    [Обрада аргумената командне линије, читање датотека и JSON приказ
    резултата.],
    [`src/lsp/`],
    [LSP сервер заснован на `tower-lsp` библиотеци и конверзија RustyJudge
    дијагностика у LSP дијагностике.],
    [`benchmarks/`],
    [Генерисање корпуса и мерење појединачних фаза анализе и укупног времена
    извршавања.],
    [`vscode-extension/`],
    [TypeScript клијент који покреће Rust LSP бинарну датотеку и повезује је са
    VS Code окружењем.]
  ),
  caption: [Главни делови RustyJudge пројекта.]
)<tbl:rustyjudge-moduli>

Табела @tbl:rustyjudge-moduli приказује поделу одговорности. Ова подела је
важна јер омогућава да се имплементација развија у слојевима. Парсер не мора да
зна како се резултат приказује у едитору. LSP сервер не мора да зна детаље
изградње CFG-а. Правила добијају припремљен контекст и враћају дијагностике.

Библиотечки улаз налази се у `src/lib.rs`. Из њега се извозе функције и типови
које користе остали делови система: покретање CLI режима, покретање LSP
сервера, parser API, `Rule`, `RuleContext`, `default_rules` и помоћне функције
из `cfg` модула. То омогућава да спољни улази буду танки слојеви око истог
језгра.

== Главни ток анализе

Главни ток анализе почиње JavaScript изворним текстом, а завршава се листом
дијагностика. Извор може доћи из датотеке, као у CLI режиму, или из отвореног
документа у едитору, као у LSP режиму. После тога оба режима користе исту
функцију `lint_file`.

#figure(
  image("../slike/arhitektura/rustyjudge-pipeline.svg", width: 95%),
  caption: [Главни ток анализе у алату RustyJudge.]
)<fig:rustyjudge-pipeline>

Слика @fig:rustyjudge-pipeline приказује централни ток. Прво се позива
`parse_source`, која враћа синтаксички корен добијен помоћу `rslint_parser`
библиотеке. Након успешног парсирања, RustyJudge покушава да изгради
`SemanticModel`. Тај корак обухвата разрешавање симбола, изградњу графова и
израчунавање података који су потребни за правила из каснијих фаза. Добијени
подаци се заједно са изворним текстом и именом датотеке смештају у
`RuleContext`. Затим `Linter` покреће сва правила и спаја њихове резултате у
једну листу дијагностика.

Посебан детаљ имплементације је да је семантички модел у контексту опционалан.
У функцији `lint_file` изградња семантичког модела се покушава, али не прекида
целу анализу ако не успе. У том случају правила која зависе само од текста или
синтаксичке структуре могу и даље да се изврше, док правила која траже
семантичке податке не пријављују резултат. Ово понашање представља облик
постепене деградације: алат не мора да одбаци целу датотеку ако један дубљи
слој анализе не може да се изгради.

Ова одлука има и ограничење. Ако семантичко спуштање не успе, правила која
користе CFG, симболе или живост немају податке на основу којих би радила. Због
тога је овај слој важан не само као имплементациона погодност, већ и као место
на ком се одређује колико дубока анализа може да буде за конкретну датотеку.

== Улазне тачке и заједничко језгро

RustyJudge има више начина покретања, али се они ослањају на исто језгро.
CLI бинарни програм налази се у `src/bin/rusty_judge.rs`. Он само позива
`run_cli()` и у случају грешке исписује поруку и враћа ненулти излазни код.
LSP бинарни програм налази се у `src/bin/rusty_judge_lsp.rs` и покреће
асинхрони `run_lsp_server()`.

#figure(
  image("../slike/arhitektura/rustyjudge-runtime.svg", width: 95%),
  caption: [Улазне тачке које користе заједничко RustyJudge језгро.]
)<fig:rustyjudge-runtime>

Слика @fig:rustyjudge-runtime приказује односе између режима рада. CLI чита
датотеке са диска и резултат исписује као JSON. LSP сервер добија текст
документа преко стандардног LSP протокола и резултат шаље назад едитору као
дијагностике. Benchmark код заобилази кориснички приказ и позива исте
библиотечке функције да би измерио појединачне фазе.

Овај распоред смањује дуплирање. Ако се измени правило или начин изградње
контекста, промена важи и за CLI и за LSP. Ако се дода ново правило у
`default_rules`, оно постаје део оба корисничка режима. Разлика између режима
није у анализи, већ у начину на који се добија улаз и приказује излаз.

== CLI режим

CLI режим је намењен покретању анализе из командне линије. Обрада аргумената
налази се у `src/cli/`. Парсер аргумената је ручно имплементиран. Подржава
директно навођење JavaScript датотека, једноставан облик директоријумског
обрасца који се завршава са `/*` и опцију `--max-line-length`. Вредност за
максималну дужину реда подразумевано је `120`, али се може променити кроз
аргумент командне линије.

Након обраде аргумената, `run_cli()` прави `Linter` са подразумеваним правилима,
чита сваку изабрану датотеку и позива `lint_file`. За сваку датотеку формира се
`FileReport`, који садржи путању датотеке и листу дијагностика. На крају се
сви извештаји серијализују помоћу `serde_json` и исписују на стандардни излаз.

Овај излазни формат је погодан за скрипте, benchmark помоћне алате или даљу
обраду. Уместо да CLI директно форматира поруке као текст за човека, он враћа
структуриране податке. Та одлука чини резултат предвидљивим и лакшим за
аутоматску анализу.

== Језгро linter-а и контекст правила

Језгро linter-а налази се у `src/linter/linter.rs`. Тип `Linter` садржи листу
правила, представљену као `Vec<Box<dyn Rule + Send + Sync>>`. Свако правило
имплементира исти интерфејс и добија исти `RuleContext`.

#figure(
```rust
pub trait Rule: Send + Sync {
    fn name(&self) -> &'static str;
    fn check(&self, rule_context: &RuleContext) -> Vec<Diagnostic>;
}
```,
  caption: [Интерфејс који имплементира свако правило.]
)<lst:rule-trait>

Листинг @lst:rule-trait приказује минимални уговор између језгра и правила.
Правило има име и функцију `check`, која враћа листу дијагностика. Сам `Linter`
не мора да зна шта правило проверава. Он само пролази кроз регистрована правила
и додаје њихове резултате у заједничку листу.

`RuleContext` је централни објекат који повезује улазне податке и анализе.
Садржи име датотеке, синтаксички корен, изворни текст, вредност за максималну
дужину реда и опциони `SemanticModel`. Поред података, контекст садржи и
помоћне методе за креирање дијагностика и приступ дубљим структурама. На тај
начин правила не морају сама да претварају распоне у редове и колоне, нити да
директно знају како је организована табела симбола.

Подразумевани скуп правила формира функција `default_rules`. У њему се налазе
правила као што су `MaxLineLength`, `NoConsoleLog`, `NoDuplicateParams`,
`NoEmptyBlock`, `StrictEquality`, `NoUnusedVars`, `NoVar`,
`NoUnreachableCode`, `ConsistentReturn`, `NoDeadStores` и `NoInfiniteLoops`.
Редослед у листи одређује редослед покретања, али правила не деле међусобно
стање. Заједнички подаци долазе из `RuleContext` објекта.

== Семантички модел као унутрашњи слој анализе

Модул `src/cfg/` представља унутрашњи слој који припрема податке за правила
која не могу да раде само над непосредним изворним текстом. Главна функција овог
модула је `build_semantic_model_from_root`. Она добија синтаксички корен и враћа
`SemanticModel`.

`SemanticModel` садржи анализу скрипт нивоа, анализе функција и табелу симбола.
Скрипт и свако тело функције имају сопствени `BodyAnalysis`, у коме се налазе
CFG и резултат анализе живости. Функције су издвојене у посебне анализе, јер
имају сопствене параметре, тело и контролни ток. Симболи се чувају у заједничкој
табели и идентификују се преко стабилних `SymbolId` вредности.

Изградња модела пролази кроз неколико имплементационих корака. Прво се прави
`Resolver`, који бележи симболе и области важења. Затим се прави `CfgBuilder`,
који постепено формира блокове и прелазе. `AstLowerer` обилази синтаксичко
стабло и преводи релевантне конструкције у интерне структуре. Када се спуштање
заврши, builder враћа CFG, над којим се рачуна живост. На крају се сви ови
подаци пакују у `SemanticModel`.

У овом поглављу није потребно детаљно описивати алгоритме изградње CFG-а и
живости, јер су они предмет наредних поглавља. За архитектуру је важније да је
овај слој изолован. Правила не граде сама графове нити сама разрешавају симболе.
Она само траже припремљене податке од `RuleContext` објекта.

== Дијагностике и излазни формати

Заједнички формат дијагностике налази се у `src/diagnostics/`. Дијагностика
садржи ред, колону и поруку. Тај формат је намерно једноставан, јер га користе
различити излазни слојеви. CLI га серијализује у JSON, док LSP сервер исти
резултат претвара у `tower-lsp` дијагностику.

Приликом креирања дијагностике правило најчешће не задаје ручно ред и колону.
Уместо тога користи методе из `RuleContext`-а, као што су `diagnostic_at` и
`diagnostic_at_span`. Оне користе распон из синтаксичког стабла или интерног
`Span` типа и претварају га у координате погодне за приказ. Оваква подела
помаже да правила остану усмерена на проверу, а не на формат излаза.

У LSP режиму свака RustyJudge дијагностика тренутно се приказује као упозорење.
Распон у LSP дијагностици почиње на реду и колони из RustyJudge дијагностике и
завршава се један карактер касније. Ово је једноставна мапа која је довољна за
приказ проблема у едитору, али оставља простор за касније прецизније означавање
целог израза или наредбе.

== LSP сервер и VS Code проширење

LSP сервер је имплементиран у `src/lsp/lsp.rs` помоћу библиотеке `tower-lsp`.
Сервер комуницира преко стандардног улаза и излаза, што омогућава да га
покреће VS Code extension као засебан процес. Унутар сервера постоји `Backend`
структура која чува LSP клијент, мапу отворених докумената, `Linter` са
подразумеваним правилима и вредност за максималну дужину реда.

Сервер подржава отварање, измену, чување и затварање документа. При отварању и
измени чува се цео текст документа. Затим се позива `lint_file`, а добијене
дијагностике се претварају у LSP формат и објављују клијенту. При затварању
документа сервер уклања текст из мапе и шаље празну листу дијагностика, чиме се
проблеми уклањају из едитора.

VS Code проширење налази се у директоријуму `vscode-extension/`. Његов TypeScript
код је танак клијент. При активацији проверава платформу и архитектуру, проналази
одговарајућу Rust LSP бинарну датотеку у `server/` директоријуму и покреће је
преко `stdio` транспорта. Проширење се активира за `javascript` и
`javascriptreact` документе. На тај начин корисничко окружење остаје одвојено од
саме анализе, која се извршава у Rust процесу.

== Benchmark интеграција

Директоријум `benchmarks/` садржи генератор JavaScript корпуса, Criterion
benchmark за RustyJudge и Node скрипте за покретање ESLint и RSLint мерења.

RustyJudge benchmark користи могућност да се библиотечке функције позову
одвојено. Због тога се могу мерити различите фазе: само парсирање,
парсирање са изградњом семантичког модела, извршавање правила над већ
припремљеним контекстом и укупан linting процес. Ова подела је могућа зато што
је архитектура система слојевита и зато што `lint_file` није једино место кроз
које се интерни делови могу употребити.

Овај део архитектуре је важан за евалуацију, али се резултати мерења не
разматрају у овом поглављу. Овде је битно само то да benchmark користи исти
код који користе CLI и LSP. Тако се избегава ситуација у којој се мери
понашање различито од стварног начина употребе алата.

== Архитектонске последице

Оваква организација има неколико практичних последица. Прва је јасно одвајање
улаза, анализе и излаза. CLI, LSP и benchmark имају различите потребе, али се
све своде на исту анализу. Друга је могућност постепеног проширивања правила:
ново правило може да користи само текст, само синтаксички корен или пуни
семантички модел, у зависности од тога шта му је потребно. Трећа је могућност
мерења појединачних фаза, што је значајно за касније поређење перформанси.

Постоје и ограничења. LSP сервер тренутно користи потпуну синхронизацију
текста, па се при свакој измени анализира цела датотека. Све LSP дијагностике
се приказују као упозорења. Конфигурација је ограничена, а вредност за
максималну дужину реда у LSP режиму је фиксна. Семантички модел је опционалан,
што омогућава наставак анализе, али може значити да дубља правила немају
довољно података. Ова ограничења не нарушавају основну архитектуру, али
представљају природне правце за касније проширење.

У наредним поглављима ова архитектура се посматра детаљније кроз два најважнија
унутрашња слоја: изградњу CFG-а и анализу живости. Тек након тога се приказује
како појединачна правила користе припремљене податке.
