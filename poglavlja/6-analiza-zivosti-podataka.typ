= Имплементација анализе живости података
<analiza-zivosti-podataka>

У RustyJudge-у анализа живости представља фазу семантичког модела која над
CFG-ом рачуна које тренутне вредности симбола могу бити прочитане у наставку
извршавања. Улаз у ову фазу је `Cfg` са основним блоковима, терминаторима и
`StatementInfo` записима у којима су већ попуњене листе `uses` и `defines`.
Излаз је `LivenessResult`, структура која садржи достижне блокове, блоковске
`use/def` скупове, `live_in/live_out` скупове и живост на нивоу појединачних
исказа.ss

== Место анализе у семантичком моделу

Главна функција анализе налази се у датотеци `src/cfg/liveness.rs` и зове се
`compute_liveness`. Позива се након што `CfgBuilder` заврши граф. То се дешава
посебно за скрипт ниво и посебно за свако тело функције. Због тога сваки
`BodyAnalysis` у семантичком моделу садржи два блиско повезана податка: `cfg`
и `liveness`.

Живост се рачуна унутар једног тела анализе. Позив функције не спаја CFG
позиваоца са CFG-ом позване функције. RustyJudge зато анализира скрипт и сваку
функцију као засебне јединице, а правила касније пролазе кроз све добијене
анализе. Модел је интрапроцедуралан: прецизан је у границама једног тела, без
изградње позивног графа целог програма.

#figure(
  image("../slike/liveness/liveness-pipeline.svg", width: 95%),
  caption: [Кораци које RustyJudge извршава при рачунању живости.]
)<fig:liveness-pipeline>

Слика @fig:liveness-pipeline приказује редослед корака. Из CFG-а се најпре
издвајају достижни блокови. Затим се за сваки блок формирају локални скупови
`use` и `def`. После тога се итеративно рачунају `live_in` и `live_out` скупови
на нивоу блокова. На основу блоковских резултата рачуна се живост на нивоу
исказа, која је неопходна за проверу да ли је конкретна додела прочитана пре
следећег уписа у исти симбол.

== Структура `LivenessResult`

Резултат анализе је структура `LivenessResult`. Она не чува имена променљивих,
већ `SymbolId` вредности. Име се добија преко табеле симбола у
`SemanticModel`-у тек када је потребно формирати поруку за корисника. Такво
представљање спречава мешање симбола који имају исто име, али припадају
различитим областима важења.

#figure(
```rust
pub struct LivenessResult {
    pub reachable: HashSet<BlockId>,
    pub block_use: Vec<HashSet<SymbolId>>,
    pub block_def: Vec<HashSet<SymbolId>>,
    pub live_in: Vec<HashSet<SymbolId>>,
    pub live_out: Vec<HashSet<SymbolId>>,
    pub stmt_live_in: Vec<Vec<HashSet<SymbolId>>>,
    pub stmt_live_out: Vec<Vec<HashSet<SymbolId>>>,
}
```,
  caption: [Скраћени приказ структуре `LivenessResult`.]
)<lst:liveness-result>

Листинг @lst:liveness-result показује да су резултати организовани као вектори.
Пошто је `BlockId` у RustyJudge-у индекс у `cfg.blocks`, исти идентификатор се
користи и као индекс у `block_use`, `block_def`, `live_in` и `live_out`
векторима. За живост исказа потребан је још један ниво индексирања: прво се
бира блок, а затим позиција исказа у том блоку.

#figure(
  table(
    columns: (1.5fr, 4.5fr),
    table.header[*Поље*][*Значење у имплементацији*],
    [`reachable`],
    [Блокови до којих постоји пут од `entry` блока. Правила га користе да
    раздвоје код који може да се изврши од кода који је остао иза прекида тока.],
    [`block_use`],
    [Симболи који се у блоку читају пре него што их тај блок дефинише.],
    [`block_def`],
    [Симболи којима блок додељује нову вредност или их уводи као дефиницију.],
    [`live_in`],
    [Симболи чија тренутна вредност може бити потребна при уласку у блок.],
    [`live_out`],
    [Симболи чија тренутна вредност може бити потребна после изласка из блока.],
    [`stmt_live_in`],
    [Симболи живи непосредно пре појединачног исказа.],
    [`stmt_live_out`],
    [Симболи живи непосредно после појединачног исказа.]
  ),
  caption: [Подаци које RustyJudge чува после анализе живости.]
)<tbl:liveness-result-fields>

Резултат има две грануларности. Блоковски скупови преносе информацију кроз
граф, док скупови на нивоу исказа омогућавају прецизније провере унутар блока.
Правило `no-dead-stores` не може да се ослони само на стање после целог блока,
јер мора да зна да ли је вредност додељена у тачно одређеном исказу жива
непосредно после тог исказа.

== Достижност као први корак

Пре саме живости RustyJudge израчунава скуп достижних блокова. Функција
`compute_reachable_blocks` креће од `cfg.entry` блока и обилази наследнике.
Наследници се добијају преко метода `BasicBlock::successors`. Тај метод спаја
редовне прелазе из терминатора и гране које воде ка `catch` блоку, ако постоје.

Скуп `reachable` има две улоге. Итеративна анализа живости не обрађује блокове
који не могу да се изврше, а правила касније користе исти резултат. На пример,
`no-unreachable-code` пријављује исказе у блоковима који нису у овом скупу, док
`no-unused-vars` игнорише употребе симбола које се налазе само у недостижном
делу кода.

Достижност зато остаје део `LivenessResult` структуре. Иако се рачуна пре
живости, њена вредност није само помоћна; она је посебан резултат семантичког
слоја који користе правила.

== Формирање `use` и `def` скупова

Након достижности, RustyJudge формира локалне скупове за сваки блок. Функција
`compute_block_use_def` пролази кроз исказе у редоследу у ком се налазе у
блоку. За сваки исказ чита симболе из `uses` и `defines` листа. Ако је симбол
употребљен пре него што је дефинисан у том истом блоку, улази у `use` скуп тог
блока. Сваки симбол који исказ дефинише улази у `def` скуп.

У овом контексту дефиниција означава упис нове вредности у симбол, а не само
прву декларацију променљиве у програму. Додела `x = 5` дефинише нову вредност
симбола `x`. Изрази `x++` и сложене доделе читају стару вредност и уписују
нову, па се исти симбол појављује и у `uses` и у `defines`.

Употребе из терминатора блока додају се у исти локални резултат. Ако је
терминатор гранање, у `use` улазе симболи из услова. Ако је терминатор
`return`, у `use` улазе симболи из повратне вредности. Услов и `return` нису
увек обични искази у листи `statements`, али и даље читају вредности које морају
бити обухваћене анализом.

== Израчунавање блоковске живости

Када су `block_use` и `block_def` попуњени, `compute_liveness` иницијализује
празне `live_in` и `live_out` скупове за све блокове. Затим покреће петљу која
се извршава док год се у некој итерацији промени бар један скуп. Унутар петље
блокови се пролазе обрнутим редоследом, што одговара смеру анализе живости:
информација се преноси од каснијих тачака програма ка ранијим.

За сваки достижан блок најпре се прави нови `live_out`. Он настаје као унија
`live_in` скупова свих достижних наследника. Пошто наследници долазе из метода
`successors`, исти код природно обухвата обичне прелазе, гранања, повратак ка
излазном блоку и поједностављене прелазе ка `catch` блоку.

Нови `live_in` почиње као копија `block_use` скупа. Затим се додају симболи из
новог `live_out` скупа, али само ако нису у `block_def` скупу тог блока.
Семантички, вредност која је потребна после блока мора бити жива и пре блока,
осим ако блок пре те употребе уписује нову вредност истог симбола. Ако се нови
скупови разликују од старих, поставља се `changed = true` и анализа наставља
још једну итерацију.

Имплементација користи директне операције над `HashSet<SymbolId>` скуповима:
копирање, унију, проверу припадности и убацивање. У коду не постоји посебна
апстракција за општи data-flow framework, јер пројекат тренутно има једну
конкретну анализу тог типа. Такво решење смањује количину инфраструктурног кода
и оставља алгоритам видљивим у самој функцији `compute_liveness`.

== Живост на нивоу исказа

Блоковска живост описује стање на границама блокова. За проверу појединачних
додела потребна је финија информација: скуп живих симбола непосредно пре и
после сваког исказа. Због тога RustyJudge после фиксне тачке позива
`compute_statement_liveness`.

Ова функција обрађује сваки блок од краја ка почетку. Почетни скуп за блок је
његов `live_out`. У тај скуп се додају и употребе из терминатора блока, јер
услов гранања или `return` читају вредности после последњег обичног исказа у
блоку. Затим се искази пролазе у обрнутом редоследу.

За сваки исказ се прво запамти тренутни скуп као `stmt_live_out`, односно стање
непосредно после исказа. Потом се из копије тог скупа уклањају симболи које
исказ дефинише, јер претходна вредност није жива пре уписа нове вредности. На
крају се додају симболи које исказ користи, јер они морају бити доступни пре
извршавања исказа. Добијени скуп постаје `stmt_live_in`.

Овај резултат повезује блоковску анализу са правилима која раде над конкретним
исказима. Блок може да има исправан `live_out`, али правило за мртве доделе
захтева податак да ли је вредност уписана у једном исказу жива одмах после тог
исказа.

== Пример мртве доделе

У примеру из листинга @lst:liveness-dead-store-example променљива `result` се
користи у `return` наредби, али вредност уписана у првом исказу не стиже до тог
читања.

#figure(
```js
function choose(a, b) {
    let result = a;
    result = b;
    return result;
}
```,
  caption: [Пример у ком је прва вредност променљиве `result` мртва.]
)<lst:liveness-dead-store-example>

Симбол `result` у овом примеру није некоришћена променљива, јер се чита у
`return result`. Мртва је само почетна вредност добијена из `a`: она се уписује
у `result`, а затим се пре првог читања замењује вредношћу `b`. Живост на нивоу
исказа раздваја употребу променљиве од употребе конкретне вредности уписане у
њу.

#figure(
  table(
    columns: (1.9fr, 1.1fr, 1.2fr, 1.4fr, 2.4fr),
    table.header[*Исказ*][*uses*][*defines*][*live-out*][*Значење*],
    [`let result = a`],
    [`{a}`],
    [`{result}`],
    [`{b}`],
    [Вредност уписана у `result` није жива после исказа.],
    [`result = b`],
    [`{b}`],
    [`{result}`],
    [`{result}`],
    [Нова вредност `result` је жива јер ће бити враћена.],
    [`return result`],
    [`{result}`],
    [`{}`],
    [`{}`],
    [`result` се чита у повратној вредности.]
  ),
  caption: [Живост на нивоу исказа за пример из листинга @lst:liveness-dead-store-example.]
)<tbl:statement-liveness-example>

== Ограничења тренутног модела

Тренутна имплементација ради на нивоу симбола. Она не прати поља објеката,
елементе низова нити алијасе. Додела `obj.x = 1` не постаје дефиниција посебног
симбола `obj.x`, јер такав симбол у моделу не постоји. Слично томе, анализа не
утврђује да ли две променљиве показују на исти објекат.

Друго ограничење је интрапроцедуралност. Живост се рачуна унутар једног тела:
скрипта или функције. Анализа не прати како вредност пролази кроз позиве
функција, нити закључује да ли ће нека функција сигурно прочитати аргумент.
За linter правила у овом пројекту то је прихватљиво ограничење, јер омогућава
стабилне локалне дијагностике без сложене међупроцедуралне анализе.

Тачност живости зависи од тачности података које је CFG слој уписао у `uses` и
`defines`. Ако нека JavaScript конструкција није моделована као дефиниција или
употреба симбола, анализа живости је неће сама открити. Проширивање подршке за
нове синтаксичке облике зато почиње у CFG слоју: када `StatementInfo` садржи
исправне дефиниције и употребе, анализа живости може да их користи без промене
основног алгоритма.

Оваква организација омогућава да правила не реконструишу ток података
самостално. Она добијају припремљен резултат семантичког модела и користе га
само за сопствену проверу.
