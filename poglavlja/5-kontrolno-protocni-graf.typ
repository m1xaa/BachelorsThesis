= Имплементација контролно-проточног графа
<implementacija-cfg>

Ово поглавље описује како је контролно-проточни граф реализован у алату
RustyJudge. Посматра се модул `src/cfg/`, улога `CfgBuilder`-а и поступак
спуштања JavaScript синтаксичког стабла у унутрашњи модел. Посебна пажња је
посвећена начину на који се граде блокови, како се бележе дефиниције и употребе
симбола и како се одвојено анализирају скрипт ниво и тела функција.

== Место CFG модула у пројекту

Сва логика везана за CFG налази се у директоријуму `src/cfg/`. Тај директоријум
садржи више датотека, али се могу издвојити четири главне групе одговорности.
Датотека `basic_blocks.rs` дефинише структуре података које представљају граф.
Датотека `builder.rs` садржи имплементацију постепене изградње графа. Датотека
`lowering.rs` повезује синтаксичко стабло са builder-ом. Датотека
`resolver.rs` чува симболе и области важења које су потребне да би се
дефиниције и употребе могле везати за стабилне идентификаторе.

#figure(
  image("../slike/cfg/cfg-construction-flow.svg", width: 95%),
  caption: [Ток изградње CFG-а унутар `src/cfg` модула.]
)<fig:cfg-construction-flow>

На слици @fig:cfg-construction-flow приказан је пут од синтаксичког корена до
структура које користе правила. `AstLowerer` је место на ком се синтаксички
чворови претварају у интерне кораке анализе. За имена и области важења ослања
се на `Resolver`, док за саме блокове и прелазе позива `CfgBuilder`. Када се
обрада једног тела заврши, резултат је `BodyAnalysis`: CFG тог тела и подаци
израчунати над њим.

== Основне структуре података

Главни тип за граф је `Cfg`. Он садржи идентификатор улазног блока,
идентификатор излазног блока и вектор свих блокова. Блокови се не чувају као
посебни објекти повезани показивачима, већ као елементи у вектору. Због тога је
`BlockId` обичан индекс типа `usize`. Овакво решење поједностављује
серијализацију, копирање и приступ блоковима током анализа.

#figure(
```rust
pub struct Cfg {
    pub entry: BlockId,
    pub exit: BlockId,
    pub blocks: Vec<BasicBlock>,
}

pub struct BasicBlock {
    pub id: BlockId,
    pub statements: Vec<StatementInfo>,
    pub terminator: Terminator,
    pub predecessors: Vec<BlockId>,
    pub exception_successors: Vec<BlockId>,
}
```,
  caption: [Скраћени приказ основних CFG структура.]
)<lst:cfg-structures>

Листинг @lst:cfg-structures приказује најважнија поља. Сваки `BasicBlock`
садржи редом додате исказе, терминатор, претходнике и посебну листу грана које
воде ка `catch` блоку. Терминатор описује нормалан завршетак блока:
безусловни прелаз, гранање, повратак из функције, недовршено стање или излазни
блок. Посебна листа `exception_successors` омогућава да се поред редовног тока
сачува и поједностављен модел преласка на обраду грешке.

Исказ унутар блока представљен је типом `StatementInfo`. Он садржи јединствени
идентификатор исказа, врсту исказа, распон у изворном коду, симболе који се
дефинишу и симболе који се користе. Ова структура је веза између CFG-а и
касније анализе живости. Builder не памти само да се нека наредба налази у
блоку, већ одмах бележи и које симболе та наредба чита или пише.

Врсте исказа су намерно сведене на оне које су потребне за анализе у пројекту:
декларација променљиве, додела, ажурирање, израз, `return`, `break`, `continue`
и празан исказ. То значи да CFG није потпуна реплика JavaScript AST-а. Он је
средњи модел који задржава информације потребне за правила, а изоставља детаље
који нису битни за тренутне анализе.

== Улога `CfgBuilder` компоненте

`CfgBuilder` је mutable компонента која постепено гради граф. При креирању
иницијализује два блока: улазни блок са идентификатором `0` и излазни блок са
идентификатором `1`. Током спуштања синтаксичког стабла builder памти тренутни
блок, следећи идентификатор исказа, стек контекста петљи и тренутни циљ за
изузетке.

Када се додаје обичан исказ, он се уписује у тренутни блок. Када се додаје
конструкција која мења ток извршавања, builder поставља терминатор и по потреби
креира нове блокове. На пример, `return` уписује исказ, поставља терминатор
`Return` и повезује тренутни блок са излазним блоком. После тога се креира нови
блок који представља наставак у тексту програма, иако тај наставак може касније
бити означен као недостижан.

Када се обрада тела заврши, метод `finish` затвара граф и враћа готов `Cfg`.
До тог тренутка builder је већ сакупио блокове, терминаторе, претходнике и
гране које воде ка обради грешке.

== Спуштање синтаксичког стабла

Поступак спуштања почиње функцијом `build_semantic_model_from_root`. Она креира
`Resolver`, `CfgBuilder` и вектор за анализе функција. Затим `AstLowerer`
обилази корен синтаксичког стабла. Када се обилазак заврши, script-level builder
се затвара, рачуна се живост за скрипт и формира се `SemanticModel`.

Пре обиласка наредби на скрипт нивоу, функцијске декларације се предекларишу.
Ово је практичан начин да се приближи JavaScript понашању у ком су функцијске
декларације доступне пре места на ком се текстуално појављују. Тек након тог
корака спуштају се појединачне наредбе.

`AstLowerer` не прави исти третман за све синтаксичке чворове. Неке конструкције
преводи у CFG исказе, неке само користи за прикупљање употреба симбола, а за
функције покреће посебан поступак. Изрази који садрже угнежђене функције се
обрађују пажљиво, да употребе симбола у унутрашњој функцији не би биле погрешно
приписане спољашњем телу.

== Декларације, доделе и употребе симбола

Када се обрађује декларација променљиве, lowerer издваја име декларатора и
евентуалну иницијалну вредност. Име се региструје у `Resolver`-у као симбол
одговарајуће врсте: `Var`, `Let` или `Const`. Ако постоји иницијализатор,
из њега се прикупљају употребе симбола. Затим builder добија позив за додавање
`VarDecl` исказа са листом дефинисаних и коришћених симбола.

Доделе се спуштају само када је лева страна једноставан идентификатор. То значи
да је израз облика `x = ...` директно подржан, док сложенији облици као што су
приступ пољу, деструктурирање или индексни приступ нису део тренутног модела.
За сложене операторе доделе, као што је `+=`, симбол са леве стране се бележи
и као употребљен и као дефинисан, јер нова вредност зависи од старе.

Ажурирања попут `x++` и `x--` представљају се као исказ који истовремено чита и
пише исти симбол. Ово је важно за каснију анализу живости и правила која
разликују корисну доделу од вредности која се никада не прочита.

== Условне наредбе

За `if` наредбу lowerer издваја услов, затим грану која се извршава када је
услов тачан и опциону `else` грану. Употребе симбола из услова се прослеђују
builder-у. Builder креира блок за `then` грану, блок за `else` грану и, ако је
потребно, заједнички блок наставка.

Ако обе гране прекидају ток, на пример обе садрже `return`, не постоји отворена
грана која треба да пређе у наставак. У том случају builder креира нови блок
наставка који може бити недостижан. Овај дизајн је користан јер каснија правила
могу да пријаве код који се налази након такве конструкције.

Услов се у CFG-у не чува као цело синтаксичко стабло израза. Чува се скуп
симбола који се у услову користе. За тренутна правила то је довољно, јер је
битно да анализа зна које вредности услов чита, а не да поново тумачи комплетан
израз.

== Петље и циљеви за `break` и `continue`

Петље захтевају додатно стање током изградње графа. `CfgBuilder` зато користи
стек `LoopContext` вредности. Сваки контекст памти `break_target` и
`continue_target`. Када се у телу петље појави `break`, builder га повезује са
циљем за излаз из петље. Када се појави `continue`, повезује га са циљем за
наставак петље, што је код `while` петље условни блок, а код `for` петље може
бити update блок.

#figure(
  image("../slike/cfg/cfg-loop-break-continue.svg", width: 90%),
  caption: [Циљеви које `CfgBuilder` користи за `break` и `continue` у петљи.]
)<fig:cfg-loop-break-continue>

Слика @fig:cfg-loop-break-continue приказује само циљеве које builder памти док
спушта тело петље. Када тело заврши нормално, ток иде ка `continue_target`.
Иста мета се користи и за `continue`, с тим што се тада прескачу преостале
наредбе у телу петље. Наредба `break` води директно у `break_target`, односно у
блок који представља наставак програма после петље.

`while` петље додатно користе једноставну процену константне истинитости услова.
Ако је услов јасно увек тачан, builder може да направи безусловни прелаз у тело
петље. Ако је услов јасно нетачан, може да пређе директно у излазни блок петље.
За све остале услове чувају се употребе симбола и гради се уобичајено гранање.
Ова процена не покушава да буде потпуна JavaScript евалуација, већ практична
помоћ за једноставне случајеве као што су `while (true)` или `while (0)`.

`for` петља се спушта у више корака. Иницијализатор се обрађује пре уласка у
структуру петље. Услов се смешта у посебан блок. Тело петље се спушта као
посебан део, а update израз добија свој блок када постоји. Ова подела омогућава
да `continue` у `for` петљи води ка update делу, а не директно на проверу
услова.

`for-in` и `for-of` петље деле заједничку функцију за спуштање. Лева страна
може бити декларација променљиве или постојећи идентификатор. Десна страна се
третира као израз чије се употребе бележе у заглављу петље. Циљна променљива
се моделује као додела која се дешава при итерацији.

== Функције као посебна тела анализе

Функције у RustyJudge-у не деле CFG са местом на ком су декларисане. Свака
функција добија сопствени builder, сопствени CFG и сопствени резултат анализе
живости. Ово важи за функцијске декларације, функцијске изразе и arrow функције.

При спуштању функције lowerer улази у нову област важења. Параметри се
декларишу као `SymbolKind::Param`. Ако је у питању именовани функцијски израз,
унутрашње име функције се такође може декларисати у одговарајућем опсегу.
Након тога се спушта тело функције. Код arrow функција са изразом као телом,
израз се моделује као имплицитни `return`.

Резултат овог поступка је `FunctionAnalysis`, који садржи име функције ако
постоји, симбол функције, распон, параметре и `BodyAnalysis`. Сви
`FunctionAnalysis` објекти чувају се у `SemanticModel.functions`. Скрипт ниво
се чува одвојено у `SemanticModel.script`.

Ова подела спречава да ток извршавања унутар функције буде помешан са током
извршавања у спољашњем коду. Декларација функције на спољашњем нивоу утиче на
табелу симбола, али тело функције има свој контролни ток.

== `try/catch` модел
RustyJudge садржи поједностављену подршку за `try/catch`. Lowerer издваја
`try` блок и `catch` блок из синтаксе. Ако постоји параметар `catch` клаузе, он
се уводи као симбол у посебној области важења за `catch` блок.

Builder током изградње `try` дела памти тренутни `catch` циљ. Када се у том
делу дода исказ или гранање, builder може да дода и грану ка `catch` блоку.
Такве гране се чувају у пољу `exception_successors`, одвојено од терминатора.
При израчунавању наследника блока, редовни прелази и прелази ка `catch` блоку
спајају се у једну листу.

Овај модел је намерно једноставан. Он омогућава да CFG има везу од кода унутар
`try` блока ка `catch` блоку, али не моделира све детаље JavaScript изузетака.
Тренутна имплементација не садржи посебан терминатор за `throw`, не моделира
`finally` блок и не разликује прецизно које наредбе стварно могу бацити изузетак.
Због тога се ова подршка може описати као приближан модел тока који прелази на
обраду грешке, а не као потпуна имплементација JavaScript exception семантике.

== Завршавање и чишћење графа

Током изградње CFG-а могу настати празни блокови. Неки су потребни као циљеви
гранања или места наставка, али неки могу остати недостижни и без исказа.
Метод `finish` зато након затварања графа позива поступак који уклања
недостижне празне блокове.

Уклањање блокова захтева пажљиво пресликавање идентификатора. Ако се блокови
избаце из вектора, сви терминатори, претходници и гране ка `catch` блоковима
морају да показују на нове идентификаторе. Због тога се прво прави мапа старих
идентификатора у нове, а затим се реконструише листа блокова и сви прелази.

Ово чишћење не уклања блокове који садрже исказе, чак и ако су недостижни.
Такви блокови су важни за правило које пријављује недостижан код. Уклањају се
само празни недостижни блокови који су настали као технички производ
конструкције графа.

== Тренутни обим подршке

Тренутни CFG слој подржава конструкције које су потребне за правила
имплементирана у RustyJudge-у. То укључује декларације променљивих, изразе,
једноставне доделе, ажурирања, `return`, блокове, `if`, `while`, `for`,
`for-in`, `for-of`, `break`, `continue`, функције и поједностављени `try/catch`.
Постоје и позната ограничења. Доделе су ограничене на једноставне идентификаторе
са леве стране. Деструктурирање, приступ својству и индексни приступ нису
моделовани као дефиниције симбола. Такође недостаје подршк за `switch`, `do-while`, `throw`, `finally`.
