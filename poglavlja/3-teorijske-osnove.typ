= Теоријске основе
<teorijske-osnove>

Ово поглавље уводи појмове који су потребни за разумевање касније
имплементације алата RustyJudge. Изворни програм се најпре посматра као текст, затим као
синтаксичка структура, потом као скуп симбола и области важења, а на крају као
граф над којим се могу рачунати особине тока извршавања и тока података.

У наставку се користи један једноставан JavaScript пример. Исти пример служи за
објашњење AST-а, CFG-а и анализе живости, како би се видело како се различите
представе односе на исти део програма.

#figure(
```js
function max(a, b) {
    let result = a;

    if (b > result) {
        result = b;
    }

    return result;
}
```,
  caption: [Једноставан JavaScript пример који се користи у овом поглављу.]
)<lst:primer-max>

Листинг @lst:primer-max садржи функцију која враћа већу од две вредности. Иако
је пример мали, у њему постоји више елемената важних за анализу: декларација
променљиве, употреба параметара, условно гранање, додела вредности и повратак
из функције. То је довољно да се прикажу основни појмови без увођења непотребне
сложености.

== Представљање изворног кода

Изворни код је на почетку само низ карактера. Да би програмски алат могао да га
анализира, тај низ мора да се преведе у структуру погодну за обраду. Први корак
је лексичка анализа. Њен задатак је да препозна токене, односно најмање
синтаксички значајне јединице програма. У примеру из листинга @lst:primer-max
токени су, између осталог, кључна реч `function`, идентификатор `max`,
заграде, зарези, кључна реч `let`, оператор `>`, оператор доделе `=` и
идентификатори `a`, `b` и `result`.

Токен садржи најмање две врсте информација. Прва је врста токена, на пример
идентификатор, број, кључна реч или оператор. Друга је положај у изворном коду.
Положај је неопходан зато што се касније дијагностике морају везати за тачно
место у датотеци. Без те информације алат би могао да закључи да проблем
постоји, али не би могао кориснику да покаже где се налази.

После лексичке анализе следи синтаксичка анализа, односно парсирање. Парсер
проверава да ли редослед токена одговара граматици језика и гради структуру
која представља програм. У компајлерској литератури ове фазе се описују као
предњи део компајлера, јер преводе текст програма у облик над којим касније
фазе могу да раде @aho2006compilers.

Важно је разликовати конкретно синтаксичко стабло и апстрактно синтаксичко
стабло. Конкретно синтаксичко стабло задржава велики број детаља о граматици,
као што су помоћни нетерминали, интерпункција и тачан облик правила. AST
изоставља детаље који нису потребни за каснију анализу и задржава суштинску
структуру програма.

== Апстрактно синтаксичко стабло

AST је стабло у коме чворови представљају програмске конструкције. Корен
обично представља целу датотеку или програм. Унутрашњи чворови представљају
конструкције као што су декларације, функције, блокови, наредбе и изрази, док
листови садрже једноставне елементе као што су идентификатори, литерали и
оператори.

#figure(
  image("../slike/teorija/ast-primer.svg", width: 88%),
  caption: [Поједностављени AST за функцију из листинга @lst:primer-max.]
)<fig:ast-primer>

Слика @fig:ast-primer приказује поједностављени AST за функцију `max`. У стварној
имплементацији стабло може да садржи више детаља, али су основни односи исти.
Функција има име, листу параметара и тело. Тело садржи декларацију променљиве,
условну наредбу и `return` наредбу. Условна наредба садржи израз који се
проверава и блок који се извршава ако је услов тачан.

AST је погодан за многе облике анализе јер чува хијерархију програма. Ако се
анализира функција, лако је пронаћи њене параметре и тело. Ако се анализира
условна наредба, лако је пронаћи услов и гране. Ако се анализира декларација,
чвор стабла обично садржи име идентификатора и почетну вредност. Због тога је
AST природна полазна тачка за linter правила, компајлерске трансформације и
алате за форматирање кода.

Међутим, AST не треба посматрати као једину представу програма. Он описује
угнежђеност синтаксичких конструкција, али не описује експлицитно све односе
који су важни за касније фазе. На пример, појава идентификатора `result` у
изразу не говори сама по себи која декларација уводи тај идентификатор. Та веза
се успоставља семантичком анализом. Слично томе, редослед чворова у стаблу није
увек исто што и редослед могућег извршавања, па се за ту сврху користи CFG.

== Идентификатори, симболи и области важења

Идентификатор је име које се појављује у програму. У JavaScript-у идентификатор
може да означава променљиву, параметар, функцију, класу или друго именовано
место у програму. Симбол је апстрактна представа тог именованог ентитета.
Разлика између идентификатора и симбола је важна: исти текст имена може да се
појави на више места, али не мора увек да означава исти симбол.

Област важења, или scope, одређује део програма у коме је одређени симбол
видљив. JavaScript има више врста области важења. Параметри функције важе у
телу функције. Променљиве декларисане са `let` и `const` имају блоковску област
важења. Променљиве декларисане са `var` нису блоковски ограничене: ако су
декларисане унутар функције, везују се за област те функције, док се на највишем
нивоу везују за одговарајући глобални или модулски контекст. Ови детаљи су део
семантике језика дефинисане ECMAScript спецификацијом @ecma262.

Семантичка анализа обично гради структуру која личи на табелу симбола. У њој
се бележи који симбол је уведен којом декларацијом, у којој области важи и на
које се појаве идентификатора односи. За пример из листинга @lst:primer-max
могу се издвојити симболи `max`, `a`, `b` и `result`. Симбол `max` представља
функцију. Симболи `a` и `b` представљају параметре функције. Симбол `result`
представља локалну променљиву декларисану у телу функције.

Разрешавање имена је поступак повезивања употребе идентификатора са одговарајућом
декларацијом. Када се у изразу `b > result` појави име `b`, анализа треба да га
повеже са параметром `b`. Када се појави `result`, анализа треба да га повеже
са локалном декларацијом `let result = a`. Ова веза је основа за правила која
се баве употребом променљивих, поновним декларацијама, сенчењем имена и другим
семантичким појавама.

Угнежђене области важења могу се посматрати као стабло. Унутрашња област може
да приступи симболима из спољашње области ако их не заклања својом декларацијом.
Ако се у унутрашњој области декларише нови симбол са истим именом, он може да
сакрије симбол из спољашње области. Због тога алат не сме да посматра само текст
идентификатора, већ мора да узме у обзир место на коме се идентификатор налази.

== Контролно-проточни граф

Контролно-проточни граф је графичка и математичка представа могућег тока
извршавања програма. Означава се као:

$ G = (V, E, "entry", "exit") $

У овој формули `V` представља скуп чворова, `E` скуп усмерених грана, `entry`
улазни чвор, а `exit` излазни чвор графа. Чворови могу да представљају
појединачне наредбе или основне блокове. Основни блок је низ наредби који има
један улаз и један излаз, без унутрашњег гранања. Гране у графу показују куда
извршавање може да пређе после неког блока.

Основни блокови се користе зато што поједностављују анализу. Уместо да се свака
наредба посматра као посебан чвор, више узастопних наредби без гранања може да
се групише у један блок. На крају блока обично се налази конструкција која
одређује следећи корак: услов, безусловни прелаз, `return`, `throw`, крај
функције или друга наредба која утиче на ток извршавања.

#figure(
  image("../slike/teorija/cfg-primer.svg", width: 90%),
  caption: [CFG за функцију из листинга @lst:primer-max.]
)<fig:cfg-primer>

Слика @fig:cfg-primer приказује CFG за функцију `max`. Блок `B0` представља
улаз у функцију и иницијализацију променљиве `result`. Блок `B1` представља
проверу услова `b > result`. Ако је услов тачан, извршавање прелази у блок
`B2`, где се променљивој `result` додељује вредност `b`. Ако услов није тачан,
прелази се директно у блок `B3`, који враћа вредност `result`. После блока
`B2` ток се такође спаја у блоку `B3`. На крају, извршавање прелази у излазни
чвор `B4`.

Условне наредбе, петље и наредбе прекида тока стварају карактеристичне облике у
CFG-у. Условна наредба има најмање две излазне гране: једну за случај када је
услов тачан и једну за случај када није. Петља обично има повратну грану која
враћа ток на проверу услова или на почетак тела петље. Наредба `return` води ка
излазу из функције и после ње се у истом току не наставља извршавање наредби
које следе у тексту програма.

CFG се користи у многим анализама јер програм претвара у структуру над којом
се могу применити алгоритми над графовима. Могуће је израчунати који су
блокови достижни, који блокови претходе неком блоку, који блокови следе после
њега и како се информације преносе кроз програм. Ови појмови су основа за
анализу тока података.

== Достижност

Достижност је својство чвора у графу. Чвор је достижан ако постоји путања од
улазног чвора до тог чвора. У контексту програма, недостижан блок представља
део кода до кога извршавање не може да стигне. Такав код је често последица
грешке, застарелог дела програма или погрешно написане контролне структуре.

Достижност се може израчунати једноставним обиласком графа од улазног чвора.
На почетку су сви чворови необележени. Улазни чвор се означава као достижан и
ставља у радну листу. Док листа није празна, узима се један чвор и обилазе се
његови наследници. Сваки наследник који још није означен додаје се у радну
листу. Када се поступак заврши, сви необележени чворови су недостижни.

#figure(
```text
worklist = [entry]
reachable = {}

while worklist is not empty:
    block = remove_one(worklist)

    if block is already in reachable:
        continue

    add block to reachable

    for successor in successors(block):
        add successor to worklist
```,
  caption: [Псеудокод алгоритма за израчунавање достижних блокова.]
)<lst:reachability>

Листинг @lst:reachability приказује основну идеју алгоритма. У пракси се могу
користити различити облици обиласка, на пример DFS или BFS, али резултат је
исти: скуп блокова који се могу достићи из улазног чвора. Овај поступак је
једноставан, али је веома користан у linter-има јер омогућава пријављивање
делова програма који се не могу извршити.

== Анализа тока података

Анализа тока података испитује како се информације мењају и преносе кроз CFG.
За разлику од достижности, која рачуна само да ли је чвор могуће посетити,
анализа тока података рачуна неку особину програма у сваком блоку. Та особина
може да буде скуп живих променљивих, скуп доступних дефиниција, скуп израза
који су већ израчунати или нека друга информација важна за анализу.

Општи облик анализе тока података може се описати помоћу улазног скупа,
излазног скупа и функције преноса. За блок `B` користе се ознаке:

- `in(B)` - информације које важе на улазу у блок;
- `out(B)` - информације које важе на излазу из блока;
- `transfer_B` - функција која описује како блок мења информације.

У најопштијем облику важи:

$ "out"(B) = "transfer"_B("in"(B)) $

Анализа може бити унапредна или уназадна. Унапредна анализа преноси информације
од почетка програма ка крају. Пример такве анализе је анализа доступних
дефиниција, где се питање поставља у облику: које дефиниције могу да стигну до
ове тачке програма. Уназадна анализа преноси информације од краја програма ка
почетку. Анализа живости је уназадна анализа, јер се питање поставља у облику:
које вредности ће бити потребне у наставку извршавања.

Када граф садржи гранања и петље, један пролаз кроз блокове обично није довољан.
Информације се морају рачунати итеративно, све док се не достигне фиксна тачка.
Фиксна тачка је стање у коме нова итерација више не мења израчунате скупове.
Пошто су скупови у овим анализама коначни, а функције преноса се дефинишу тако
да монотоно додају или уклањају информације у контролисаном облику, итеративни
поступци су стандардни начин израчунавања резултата @nielson1999principles.

== Анализа живости

Променљива је жива у некој тачки програма ако њена тренутна вредност може бити
употребљена на некој путањи извршавања која почиње у тој тачки. Ако вредност
није жива, њено чување или додељивање може бити непотребно. Због тога је
анализа живости важна у компајлерима, оптимизацијама и linter правилима.

Анализа живости користи два локална скупа за сваки блок:

- `use(B)` - променљиве које се у блоку користе пре него што се у том блоку
  дефинишу;
- `def(B)` - променљиве којима се у блоку додељује вредност или које се у
  блоку дефинишу.

Поред њих рачунају се и два глобална скупа:

- `in(B)` - променљиве живе на улазу у блок;
- `out(B)` - променљиве живе на излазу из блока.

Пошто је живост уназадна анализа, `out(B)` зависи од улазних скупова
наследника блока `B`:

$ "out"(B) = union_(S in "succ"(B)) "in"(S) $

Скуп `in(B)` добија се тако што се узму променљиве које се користе у самом
блоку и додају променљиве које су живе после блока, осим оних које се у блоку
поново дефинишу:

$ "in"(B) = "use"(B) union ("out"(B) - "def"(B)) $

У овој формули `succ(B)` означава наследнике блока `B`, а знак `-` представља
разлику скупова. Интуиција је следећа: ако се променљива користи у блоку пре
нове дефиниције, она мора бити жива на улазу у блок. Ако је променљива жива на
излазу из блока, она је жива и на улазу, осим ако блок пре тога дефинише нову
вредност која замењује стару.

#figure(
```text
for each block B:
    in[B] = {}
    out[B] = {}

repeat:
    changed = false

    for each block B in reverse order:
        old_in = in[B]
        old_out = out[B]

        out[B] = union of in[S] for every successor S of B
        in[B] = use[B] union (out[B] - def[B])

        if in[B] != old_in or out[B] != old_out:
            changed = true
until changed == false
```,
  caption: [Псеудокод итеративне анализе живости.]
)<lst:liveness-algorithm>

Листинг @lst:liveness-algorithm приказује типичан итеративни алгоритам. Блокови
се често обилазе обрнутим редоследом у односу на ток извршавања, јер је анализа
уназадна. Тај редослед није услов за исправност, али може да убрза достизање
фиксне тачке.

#figure(
  table(
    columns: (0.7fr, 1.5fr, 1.3fr, 1.5fr, 1.5fr, 2.5fr),
    table.header[*Блок*][*Наредба*][*use*][*def*][*in*][*out*],
    [B0], [`result = a`], [`{a}`], [`{result}`], [`{a, b}`], [`{b, result}`],
    [B1], [`b > result`], [`{b, result}`], [`{}`], [`{b, result}`], [`{b, result}`],
    [B2], [`result = b`], [`{b}`], [`{result}`], [`{b}`], [`{result}`],
    [B3], [`return result`], [`{result}`], [`{}`], [`{result}`], [`{}`],
    [B4], [`exit`], [`{}`], [`{}`], [`{}`], [`{}`],
  ),
  caption: [Скупови анализе живости за CFG са слике @fig:cfg-primer.]
)<tbl:liveness-example>

Табела @tbl:liveness-example приказује резултат анализе за пример `max`. На
улазу у блок `B0` живи су параметри `a` и `b`. Вредност `a` је потребна за
иницијализацију променљиве `result`, а вредност `b` је потребна касније у
услову и могућој додели. На излазу из блока `B0` жива је вредност `result`,
али и параметар `b`, јер ће се оба користити у услову `b > result`. После блока
`B2` жива је само вредност `result`, јер се она враћа у блоку `B3`.

Овакав резултат показује како се локалне информације из појединачних блокова
повезују са глобалном структуром графа. У блоку `B2` променљива `result` се
дефинише, па стара вредност `result` није жива на улазу у тај блок. Међутим,
нова вредност јесте жива на излазу, јер се користи у `return` наредби. Управо
ова врста разликовања старе и нове вредности чини анализу живости корисном за
правила која испитују употребу резултата доделе.

== Дијагностике и позиције у изворном коду

Резултат статичке анализе није користан ако се не може јасно приказати
кориснику. Зато алати за анализу кода производе дијагностике. Дијагностика је
структурирана порука која обично садржи:

- идентификатор правила;
- ниво озбиљности;
- поруку за корисника;
- позицију у изворном коду;
- опциони предлог исправке или додатно објашњење.

Позиција у изворном коду најчешће се представља као распон. Распон може бити
задат помоћу почетног и крајњег индекса у датотеци, или помоћу броја реда и
колоне. За приказ у едитору обично је потребно претворити интерне индексе у
редове и колоне, јер корисник проблем види у том облику. Ако је анализа
извршена над AST-ом или CFG-ом, сваки чвор који може да произведе дијагностику
треба да задржи везу са делом изворног кода из кога је настао.

Добра дијагностика треба да буде кратка, али довољно прецизна. Порука не треба
само да каже да је правило прекршено, већ треба да помогне програмеру да разуме
зашто је место означено. Код linter-а је то посебно важно јер се дијагностике
појављују током писања кода. Ако су поруке нејасне или ако означавају превелики
део програма, корисник теже прихвата алат као део редовног рада.
