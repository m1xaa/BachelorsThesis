= CLI и VS Code интеграција
<cli-vscode-integracija>

RustyJudge има два корисничка улаза: покретање из командне линије и рад у
едитору преко VS Code проширења. Оба режима користе исто језгро анализе. Разлика
је у начину на који добијају изворни текст и у облику у ком приказују
дијагностике.

== CLI режим

CLI бинарна датотека налази се у `src/bin/rusty_judge.rs`. Њена улога је мала:
позива `run_cli`, исписује грешку ако до ње дође и враћа ненулти излазни код.
Сама обрада аргумената налази се у `src/cli/`, док се извршавање анализе налази
у `src/run_cli/run.rs`.

CLI прихвата путање до `.js` датотека и једноставан облик директоријумског
обрасца који се завршава са `/*`. У том случају се из директоријума узимају
само JavaScript датотеке. Подржана је и опција `--max-line-length`, која мења
границу за правило `max-line-length`. Подразумевана вредност је `120`.

После обраде аргумената прави се `Linter` са подразумеваним правилима. За сваку
датотеку чита се изворни текст, позива се `lint_file`, а резултат се смешта у
`FileReport`. На крају се цела листа извештаја исписује као JSON. Такав излаз
је погодан за benchmark скрипте и аутоматску обраду, јер није везан за
конзолни приказ намењен само човеку.

#figure(
```text
[
  {
    "file": "example.js",
    "diagnostics": [
      { "row": 3, "col": 5, "message": "..." }
    ]
  }
]
```,
  caption: [Облик CLI резултата.]
)<lst:cli-json-output>

CLI режим је користан и као најједноставнији начин провере рада алата. Не
зависи од едитора, LSP комуникације или VS Code паковања, па је погодан за
тестирање језгра анализе над конкретним датотекама.

== LSP сервер

За рад у едитору RustyJudge користи Language Server Protocol. LSP раздваја
едитор од алата који анализира код. Едитор шаље догађаје о документима, а
сервер враћа одговоре у стандардном облику, најчешће као дијагностике,
аутодопуне, дефиниције или друге језичке функционалности. У RustyJudge-у је
имплементиран само део потребан за дијагностике.

LSP бинарна датотека налази се у `src/bin/rusty_judge_lsp.rs`. Покреће
`run_lsp_server` у `tokio` runtime-у. Сервер је реализован помоћу библиотеке
`tower-lsp`, која обезбеђује структуру за JSON-RPC комуникацију и Rust trait
`LanguageServer`. Комуникација иде преко стандардног улаза и излаза, што је
уобичајен начин покретања language server-а из едитора.

Главна структура LSP дела је `Backend`. Она чува `Client`, мапу отворених
докумената, један `Linter` и вредност за максималну дужину реда. Документи се
чувају у `RwLock<HashMap<Url, String>>`, јер LSP методе раде асинхроно и више
догађаја може приступати истом стању.

При иницијализацији сервер објављује да подржава отварање и затварање
докумената, чување и `FULL` синхронизацију измена. То значи да при промени
документа сервер добија цео нови текст, а не само разлику између две верзије.
Ово је једноставније за имплементацију и довољно је за величину и циљ пројекта.

Када се документ отвори или измени, сервер ажурира текст у мапи и позива
`lint_and_publish`. Та функција покреће `lint_file` над тренутним текстом,
претвара RustyJudge дијагностике у LSP дијагностике и шаље их едитору преко
`publish_diagnostics`. Ако парсирање или анализа врати грешку, сервер шаље
једну дијагностику на почетак датотеке са поруком о синтаксичкој грешци.

Конверзија дијагностике је директна. RustyJudge користи ред и колону који су
индексирани од један, док LSP користи позиције индексиране од нуле. Зато се
при конверзији обе вредности умањују за један. Све дијагностике се шаљу као
`WARNING`, са извором `rusty-judge`. Распон тренутно покрива један карактер од
позиције дијагностике.

Када се документ затвори, сервер га уклања из мапе и шаље празну листу
дијагностика. То чисти приказ проблема у едитору за документ који више није
отворен.

== VS Code проширење

VS Code проширење се налази у директоријуму `vscode-extension/`. Клијент је
написан у TypeScript-у, у датотеци `src/extension.ts`. Његов задатак није да
анализира JavaScript, већ да пронађе RustyJudge LSP бинарну датотеку, покрене
је и повеже је са VS Code LSP клијентом.

Проширење се активира за језике `javascript` и `javascriptreact`. При активацији
одређује платформу и архитектуру процеса, на пример `win32-x64`, и на основу
тога гради очекивану путању до server бинарне датотеке. На Windows-у очекује
`rusty_judge_lsp.exe`, док на осталим системима очекује `rusty_judge_lsp`.
Ако платформа није подржана или бинарна датотека не постоји, кориснику се
приказује грешка.

Када је бинарна датотека пронађена, проширење прави `ServerOptions` са
`TransportKind.stdio`. Затим прави `LanguageClient` са selector-ом за
JavaScript документе и покреће клијент. Од тог тренутка VS Code шаље LSP
догађаје серверу, а сервер враћа дијагностике које се приказују у едитору и у
панелу Problems.

Паковање проширења ослања се на `package.json`, `esbuild` и `vsce`. TypeScript
код се компајлира и пакује у `dist/extension.js`, а LSP бинарна датотека мора
бити присутна у одговарајућем `server/<platform>/` директоријуму пре паковања
VSIX архиве. На тај начин VS Code проширење остаје танак клијент, док се сва
анализа и даље извршава у Rust бинарном програму.

Ова организација задржава један извор истине за правила. CLI, LSP сервер и
VS Code проширење не садрже различите имплементације провера. Они се разликују
само по начину покретања и приказа резултата.
