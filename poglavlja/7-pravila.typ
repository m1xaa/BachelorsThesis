= Имплементација правила
<implementacija-pravila>

Скуп подразумеваних правила у RustyJudge-у покрива три нивоа провере: директну
проверу текста, проверу синтаксичког стабла и проверу података добијених из
семантичког слоја. Разлика између ових група одређује које податке правило
тражи од `RuleContext` објекта и колико је дубока анализа потребна да би
дијагностика била поуздана. Сва правила враћају исту врсту резултата: листу `Diagnostic` објеката.

== Преглед имплементираних правила

Подразумевани скуп правила формира се у функцији `default_rules`. Сва правила
су независна: не мењају заједничко стање и не позивају једно друго. Заједнички
су им само улазни контекст и формат дијагностике.

#figure(
  table(
    columns: (1.6fr, 2.1fr, 3.3fr),
    table.header[*Правило*][*Потребни подаци*][*Услов за дијагностику*],
    [`max-line-length`],
    [Изворни текст и `max_line_length`.],
    [Ред има више карактера од дозвољене границе.],
    [`no-console-log`],
    [Токени из синтаксичког стабла.],
    [Појављује се низ токена `console`, `.`, `log`.],
    [`no-duplicate-params`],
    [`PARAMETER_LIST` чворови и идентификатори у њима.],
    [Исто име параметра појављује се више пута у истој листи параметара.],
    [`no-empty-block`],
    [`BLOCK_STMT` чворови и њихови токени.],
    [После `{` следи `}` без садржаја, уз игнорисање whitespace токена.],
    [`strict-equality`],
    [Токени оператора.],
    [Користи се `==` или `!=` уместо `===` или `!==`.],
    [`no-var`],
    [`VAR_DECL` чворови.],
    [Декларација користи кључну реч `var`.],
    [`no-unused-vars`],
    [Табела симбола, употребе симбола и достижност.],
    [Симбол проверљиве врсте нема употребу у достижном коду.],
    [`no-unreachable-code`],
    [CFG и скуп достижних блокова.],
    [Исказ се налази у блоку који није достижан од улаза у тело.],
    [`consistent-return`],
    [CFG функције, терминатори и достижни претходници излазног блока.],
    [Неке достижне путање враћају вредност, а друге не враћају вредност.],
    [`no-dead-stores`],
    [Живост на нивоу исказа.],
    [Исказ уписује вредност у симбол који није жив после тог исказа.],
    [`no-infinite-loops`],
    [CFG, достижност, SCC компоненте и пут до излазног блока.],
    [Циклична компонента нема достижан излаз из тела анализе.]
  ),
  caption: [Правила и подаци који су им потребни.]
)<tbl:pravila-podaci>

== Правила над текстом и синтаксом

Правило `max-line-length` ради директно над изворним текстом. Обилази редове
из `RuleContext.source` и пореди дужину реда са вредношћу
`RuleContext.max_line_length`. Дијагностика се поставља у колону која прва
прелази дозвољену границу. Ово правило је независно од JavaScript синтаксе:
једнако ради над сваким редом који стигне до linter-а.

Правило `strict-equality` користи синтаксичке токене. Обилази елементе стабла и
проверава да ли је врста елемента `EQ2` или `NEQ`. Прво одговара оператору
`==`, а друго оператору `!=`. У оба случаја правило пријављује позицију самог
оператора. Не анализира типове израза, јер циљ правила није да процени да ли је
нестрого поређење у конкретном случају безбедно, већ да наметне доследан стил
поређења.

Правило `no-var` тражи `VAR_DECL` чворове и у њима кључну реч `var`. Провера је
синтаксичка, јер је употреба `var` видљива већ у AST-у. За дијагностику није
потребно знати у којој области важења је променљива декларисана, нити да ли се
касније користи.

Правило `no-console-log` обилази токене и тражи тачан низ `console`, `.`, `log`.
Због такве имплементације оно пријављује и израз `console.log` без заграда, не
само позив `console.log(...)`.

Правило `no-empty-block` ради над `BLOCK_STMT` чворовима. После леве витичасте
заграде прескаче whitespace токене и проверава да ли је следећи токен десна
витичаста заграда.

Правило `no-duplicate-params` обилази сваку листу параметара посебно. За једну
`PARAMETER_LIST` структуру прави локални `HashSet<String>` и у њега додаје
имена параметара. Ако се име већ налази у скупу, пријављује се дијагностика на
поновљеном идентификатору. Скуп се прави поново за сваку листу параметара, па
исто име у две различите функције није проблем.

== Правила над симболима и достижношћу

Правило `no-unused-vars` користи табелу симбола и податке о употребама симбола.
Прво сакупља све симболе који се читају у достижним блоковима. У обзир улазе
`uses` листе исказа, употребе из услова гранања и употребе из `return`
терминатора. Након тога правило пролази кроз симболе из табеле и пријављује
оне који припадају проверљивим врстама: `Var`, `Let`, `Const`, `Param` и
`Function`.

Достижност је важна за ово правило зато што употреба у коду који се не може
извршити не треба да сакрије проблем. На пример, ако се променљива чита само
после `return` наредбе, та употреба се не рачуна. Правило ради над `SymbolId`
вредностима, па сенчење имена не прави лажне резултате: две променљиве са истим
текстуалним именом остају различити симболи.

Правило `no-unreachable-code` користи исти скуп достижних блокова, али на
директнији начин. Пролази кроз блокове сваког тела анализе и пријављује сваки
исказ у блоку који није достижан. Порука се везује за `Span` исказа. Ако је
недостижан код у функцији, порука садржи и име функције када је оно доступно.

== Правила над повратним путањама

Правило `consistent-return` проверава само функције. За сваку функцију посматра
претходнике излазног блока који су достижни. Такви претходници описују начине
на које извршавање може да напусти функцију: преко `return` терминатора, преко
обичног пада на крај функције или преко гранања које води директно ка излазу.

Правило бележи две појаве. Прва је постојање достижне путање која завршава
`return` наредбом са вредношћу. Друга је постојање достижне путање која завршава
`return` наредбом без вредности или доласком до краја функције без експлицитног
повратка вредности. Дијагностика се пријављује само када постоје обе појаве у
истој функцији.

== Правило за мртве доделе

Правило `no-dead-stores` је најдиректнији корисник живости на нивоу исказа.
Проверава само исказе који уписују вредност: декларације са иницијализатором,
доделе и update изразе. То је издвојено у функцији `is_store_statement`.

#figure(
```rust
fn is_store_statement(kind: &StatementKind) -> bool {
    matches!(
        kind,
        StatementKind::Assign
            | StatementKind::Update
            | StatementKind::VarDecl { has_initializer: true }
    )
}
```,
  caption: [Врсте исказа које правило `no-dead-stores` посматра као упис вредности.]
)<lst:dead-store-write-like>

За сваки такав исказ правило чита `stmt_live_out` скуп. Ако симбол који је
исказ дефинисао није жив после исказа, додељена вредност не може бити прочитана
пре следећег уписа или краја тела. Тада се пријављује дијагностика на распон
исказа. Недостижни блокови се прескачу, јер се за њих већ може пријавити
проблем недостижног кода.

#figure(
```js
let x = 1;
x = 2;
console.log(x);
```,
  caption: [Пример у ком је прва додела променљивој `x` мртва.]
)<lst:dead-store-rule-example>

У листингу @lst:dead-store-rule-example прва вредност променљиве `x` не стиже
до читања. Друга додела је корисна, јер се управо та вредност чита у
`console.log(x)`. Правило зато не посматра само да ли се променљива негде
користи, већ да ли се користи конкретна вредност настала датим уписом.

== Правило за бесконачне петље

Правило `no-infinite-loops` користи структуру CFG-а као граф. За свако тело
анализе прво рачуна блокове из којих се може стићи до излазног блока. То ради
обрнутим обиласком преко `predecessors` листа, почевши од `cfg.exit`. Затим
израчунава јако повезане компоненте над достижним делом графа.

Компонента се сматра цикличном ако садржи више од једног блока или ако једини
блок у компоненти има грану ка самом себи. Ако ниједан блок из такве компоненте
не може да стигне до излазног блока, правило пријављује потенцијалну
бесконачну петљу.

Ова провера не покушава да докаже све могуће облике бесконачног извршавања.
Она препознаје ситуације у којима CFG нема излазну путању из циклуса. Због тога
петља са достижним `break` прелазом неће бити пријављена, чак и ако би услови у
реалном извршавању могли спречити да се тај `break` икада достигне. То је
свесно ограничење: правило се ослања на структуру путања, а не на вредносну
анализу услова.

== Дијагностике и тестови

Дијагностичке поруке се формирају у уједначеном облику, независно од тога из
ког слоја анализе потиче информација о позицији. Када правило утврди да је
потребно пријавити проблем, оно доставља релевантан опсег и текстуалну поруку,
a остали слојеви их користе као заједнички излаз. На тај начин CLI, LSP и други
корисници добијају исте дијагностике без обзира на то да ли је проблем
откривен на основу текста, синтаксе или семантичке анализе.

Поред тога, сваки модул са правилима прати и тестове који проверавају да ли
правило правилно реагује у основним ситуацијама. Тестови обухватају случајеве
када проблем треба пријавити, случајеве када га не треба пријавити и граничне
случајеве који помажу да се заштити понашање правила у нестандардним
условима.